# Offline model-training dependencies. NOT needed to run Argus.
#
# `scripts/train_form_model.py` refits an exercise's form classifier (plank,
# bicep, lunge -- see docs/ADDING_AN_EXERCISE.md) and writes a coefficient
# artifact; the artifact is what ships. Neither the laptop server nor the
# phone imports any of this -- the phone evaluates the model as a dot product
# in Kotlin, and `argus.triage` never sees it at all. Keeping these out of
# requirements.txt is what lets the import-hygiene CI job assert the scorer
# pulls in no ML runtime.
#
# Install only when retraining:  pip install -r requirements-train.txt
scikit-learn>=1.3
numpy>=1.24
