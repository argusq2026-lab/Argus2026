# Argus core dependencies.
#
# Deliberately small: the triage scorer is pure stdlib, and pose/form
# classification now run on each trainee's phone (see docs/PROTOCOL.md), so
# there is no model runtime, no OpenCV, and no camera dependency here at all.
# The only runtime dependency is the WebSocket ingest server.
websockets>=13

# Test suite.
pytest>=7.0
pytest-timeout>=2.1
